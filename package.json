{
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "install": "npm install -g web-component-tester",
    "test": "wct",
    "build": "jspm bundle-sfx src/augeas - jsonld augeas.js --global-name augeas --format umd --minify --globals \"{ 'jsonld': 'jsonld' }\""
  },
  "jspm": {
    "directories": {
      "test": "tests"
    },
    "dependencies": {
      "jsonld": "npm:jsonld@^0.4.11"
    },
    "devDependencies": {
      "typescript": "npm:typescript@^1.6.2"
    }
  }
}
