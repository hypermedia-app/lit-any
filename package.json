{
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "install": "npm install -g web-component-tester wct-local wct-sauce bower typings",
    "test": "wct --plugin sauce",
    "test-local": "wct -l chrome -p --skip-selenium-install",
    "build": "twc --out-dir dist  "
  },
  "devDependencies": {
    "twc": "^0.1.9"
  }
}
